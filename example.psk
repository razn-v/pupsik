fnc main: void -> int64 {
    let str hello = "Hello, world!";
    @printf("%s\n", hello);

    let a = 0;
    let int64 b;

    @printf("%d %d\n", a, b);
    b = 2;
    @printf("%d %d\n", a, b);

    for let int64 i; i < 10; i = i + 1 {
        @printf("%d\n", i);
    };

    return 0;
}
